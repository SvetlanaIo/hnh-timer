/**
 * Порядок сортировки.
 *
 * @export
 * @enum {number} Множитель порядка сортировки.
 */
export enum SortOrder {
  /**
   * По возрастанию.
   */
  Ascending = 1,

  /**
   * По убыванию.
   */
  Descending = -1,
}
